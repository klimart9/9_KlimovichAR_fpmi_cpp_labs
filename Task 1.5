#include <iostream>

int main() {
    int n, m;
    std::cout << "Dva Chisla: ";
    std::cin >> n >> m; 
    std::cout << "Deliteli " << n << " and " << m << ": ";

    int min = (n < m) ? n : m;

    for (int i = 1; i <= min; i++) {
        if (n % i == 0 && m % i == 0) {
            std::cout << i << " ";
        }
    }

   
    return 0;
}
